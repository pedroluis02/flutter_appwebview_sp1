name: flutter_appwebview_sp1
description: Embedded webview inside app
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  webview_flutter: ^3.0.4
  flutter_inappwebview: ^5.4.3+7
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true